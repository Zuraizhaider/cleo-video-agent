[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt && apt-get install -y ffmpeg || true"

[deploy]
startCommand = "python3 bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
